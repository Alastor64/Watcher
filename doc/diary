1.19重载矩阵加法时，左右值把我搞红温了，虽然拷贝时间n方比起乘法微不足道，但我还是想避免一切不必要的拷贝赋值、构造。
    所以我突击了一下C++的左右值，返回值很好解决，用move就行。
    参数没想到好的方法，我选择重载4个加法（左左，左右，右左，右右）。
    然后写减法的时候傻眼了。
    算了，我不优化减法了。
    啊这，右值引用有了名字就变成左值，高人一等了吗？让我想起了千与千寻。
    move真好用。
    那我好像只要两个重置就行。没错。
1.20我要改进bookstore作业里的memoryriver，让它支持多态类。
    先实现基本的FileBase，仅支持返回文件数据区大小，读写加任意长度。
    在此基础上实现File<T>，把T按数组方式存储，
    能在下标为index（0-base）处连续读写多个类。
    最后实现FileSingle<T>，仅能单个添加，读取，修改类，额外支持删除（带空间回收）
    我觉得有必要测一下文件读取少量多次和多量少次的区别。